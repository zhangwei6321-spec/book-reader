name: Render 保活

on:
  schedule:
    - cron: '*/10 * * * *'  # 每 10 分钟

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -s -o /dev/null -w "HTTP %{http_code}\n" https://media-parser-bfmx.onrender.com/health
